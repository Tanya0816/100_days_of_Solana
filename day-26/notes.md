This is day 26 of learning Solana 

While understanding how solana explorer works-

I saw Live Transaction State where the count of transaction kept increasing every nano second. 

Q.How do Solana manages to keep count with the number of transactions happening every second? We know every transaction goes through a process of getting approved by the nework before actually getting added in the network. 


Solana uses pipelining,stake-based leadership and proof oh history to approve the transactions at a blistering speed.

 Solana allows parallel processing which causes consensus process to take very less time. The system streams transactions into continuous blocks and counts as successfully pass through the validator pipeline.

The pipeline words similar to how passengers are checked at the airport. Every guard has it's own uniqu role which help in fast checking.